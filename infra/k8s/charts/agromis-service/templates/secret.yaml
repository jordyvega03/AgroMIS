# Este archivo es un placeholder.
# Los secrets reales se crean via External Secrets Operator desde AWS Secrets Manager.
# Ver: infra/k8s/external-secrets/
#
# Para testing local se puede crear un Secret manual:
# kubectl create secret generic my-service-secrets --from-env-file=.env.local
