apiVersion: v1
kind: Service
metadata:
  name: {{ include "agromis-service.fullname" . }}
  labels:
    {{- include "agromis-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agromis-service.selectorLabels" . | nindent 4 }}
